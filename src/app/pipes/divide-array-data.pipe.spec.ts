import { DivideArrayDataPipe } from './divide-array-data.pipe';

describe('DivideArrayDataPipe', () => {
  it('create an instance', () => {
    const pipe = new DivideArrayDataPipe();
    expect(pipe).toBeTruthy();
  });
});
